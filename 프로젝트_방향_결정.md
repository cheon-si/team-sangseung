# 막차 프로젝트 방향 결정 및 데이터 정리

2026 통계최강자전 · 팀 상승 · 2026-09-16 기준

이 문서는 세 가지를 한 곳에 모은 것입니다. 모델링 방향을 어떻게 정했는지, 어떤 변수를 쓸 수 있는지, 확보한 데이터가 어디까지 검증됐는지.

---

## 1. 목표

상황(노선, 요일, 시각, 환승 조건)을 넣으면 **"이 환승에서 막차를 갈아탈 확률"**을 돌려주는 모형과 프로토타입.

---

## 2. 방법론 결정

### 원안

실측 지연과 시간표·환승거리로 계산한 필요 버퍼시간을 비교해 성공/실패 Y를 만들고, 로지스틱 회귀로 P(성공)을 추정. 조합별 관측치가 적으니 노선·요일 단위 지연 분포에서 몬테카를로로 가상 지연을 대량 생성해 학습 데이터를 보강하고, 분포 추정의 불확실성은 붓스트랩으로 신뢰구간 제시.

### 원안의 문제

Y는 "지연 D가 버퍼 B보다 작으면 1"이므로 P(Y=1 | X) = F_D(B | X), 즉 지연 분포의 누적분포함수를 버퍼 지점에서 읽은 값입니다. 로지스틱 회귀는 그 함수가 로짓 척도에서 선형이라는 가정을 얹은 근사이고, 지연 분포를 직접 추정하면 그 가정 없이 같은 확률이 나옵니다.

시뮬레이션 단계는 순환입니다. 실측으로 F_D를 추정하고, 거기서 가상 D를 뽑아 가상 Y를 만들고, 그걸로 로지스틱을 학습하면 결과는 F_D(B)로 되돌아갑니다. 처음 추정한 분포보다 나은 정보가 나올 수 없고, 합성 표본 만 개에서 나온 표준오차와 p값은 실제 표본이 아니라 시뮬레이션 횟수를 반영하므로 보고할 수 없는 숫자가 됩니다.

시뮬레이션이 실제로 하는 일은 풀링입니다. "노선·요일 단위 분포로 부족한 조합을 채운다"는 것은 역 단위 지연이 노선 단위 지연과 같다는 가정이고, 이건 가정으로 명시하고 P(성공 | 역 s) = F_D,노선(B_s)를 계산하면 됩니다.

### 결정

**지연 분포를 직접 적합하고, 성공 확률은 그 분포에서 버퍼 지점을 읽어 구합니다.** 시뮬레이션으로 학습 데이터를 만들어 로지스틱에 넣는 단계는 뺍니다.

| 단계 | 내용 |
|---|---|
| 지연 분포 | 노선 × 평일/주말 × 시간대별로 적합. 오른쪽 꼬리가 길어 로그정규 혼합이나 감마가 맞고, 표본이 충분하면 경험적 CDF도 됨 |
| 성공 확률 | P(성공) = F_D(B). B는 시간표와 환승 소요시간에서 결정론적으로 나옴 |
| 불확실성 | 밤 단위 클러스터 붓스트랩. 같은 밤 안의 열차들은 지연이 전파되어 상관되므로 밤을 재추출 |
| 검증 | leave-one-night-out으로 보정 곡선과 Brier score. 시간표만 쓴 결정론적 예측(지연 0 가정)을 기준선으로 두고 개선폭 확인 |
| 로지스틱 | 실측 Y에만 적합하는 비교 모형으로 남김. 합성 데이터로 부풀리지 않음 |

원안에서 유지하는 것: Y를 실측 지연과 필요 버퍼 비교로 만드는 정의, X 변수 구성, 노선 단위로 풀링하는 판단, 붓스트랩으로 신뢰구간을 붙이는 방향.

### 유의성에 대해

노선 단위로 수만 건이 쌓이면 노선 간 차이는 전부 유의하게 나옵니다. 이 문제는 검정이 아니라 추정과 예측 보정입니다. 물어야 할 것은 예측 확률이 관측 빈도와 맞는가, 밤을 하나씩 빼도 유지되는가, 시간표만 쓴 것보다 나은가입니다.

### 표본 수

시간표로 계산한 하룻밤 22시~01시 창의 관측치입니다.

| 단위 | 하룻밤 | 4주 |
|---|---:|---:|
| 열차 도착 이벤트 (전 노선) | 14,784 | 약 295,000 |
| 노선당 도착 이벤트 | 615 ~ 2,682 | 12,000 ~ 54,000 |
| 환승역 막차 도착 (역×노선×방향) | 243 | 평일 3,888 + 주말·공휴일 2,916 |
| 조합당 막차 관측 | 1 | 평일 16, 주말·공휴일 12 |

노선 단위 지연 분포는 표본이 넉넉합니다. 역 단위 막차 성공 여부는 조합당 밤 하나씩이라 어떤 방법으로도 역별 계수를 뽑을 수 없습니다. 희소성은 실재하지만 시뮬레이션이 아니라 노선 단위 풀링으로 푸는 문제입니다.

24시대 열차가 22시대의 절반입니다. 지연이 밤새 누적되는지 소멸되는지에 따라 막차 지연 분포가 22시 분포와 다를 수 있으므로 시간대를 조건에 넣습니다.

### 측정 오차

수집 간격이 2분입니다. 도착 시각을 폴링 시각으로 잡으면 지연 측정 오차가 최대 2분인데, 시간표 EDA에서 환승 여유시간 중앙값이 -1.1분, 최빈값이 0분이었습니다. 결정 마진과 측정 오차가 같은 자릿수입니다.

일괄 도착 API의 `barvlDt`(도착까지 남은 초)로 도착 시각을 보간하면 분 단위 아래로 내려갈 수 있습니다. 첫날 밤 데이터로 이게 실제로 정확한지 확인하고, 안 되면 24시~01시 구간만 1분 간격으로 올립니다.

### 선행연구

Stratil-Sauer와 Breyer(2025)가 환승이 있는 열차 여정의 지연을 확률적으로 모형화했습니다. 지연 분포에 두 로그정규 혼합을 써서 드물게 크게 늦는 꼬리를 잡았고, 환승 신뢰도는 별도 모형으로 두고 다른 역 데이터로 일반화를 검증했습니다. 지연 분포를 직접 모형화하고 환승 확률은 거기서 도출하는 구조가 이번 결정과 일치합니다.

국내는 한국철도학회에 서울시 실시간 지하철 Open API로 열차 출·도착 시각을 수집한 시스템 개발 논문이 있어 수집 설계의 근거로 쓸 수 있습니다.

- [Probabilistic modeling of delays for train journeys with transfers (arXiv 2504.17479)](https://arxiv.org/abs/2504.17479)
- [Reliability and delay distributions of train connections](https://www.researchgate.net/publication/265484179_Reliability_and_delay_distributions_of_train_connections)
- [Complex delay dynamics on railway networks](https://arxiv.org/pdf/1707.08632)
- [열차 지연 예측을 위한 DEVS 기반 시뮬레이션 모델 개발](https://www.kci.go.kr/kciportal/ci/sereArticleSearch/ciSereArtiView.kci?sereArticleSearchBean.artiId=ART003253860)
- [서울시 실시간 지하철 Open API 시스템을 활용한 열차 출·도착시각 수집 시스템 개발](https://www.dbpia.co.kr/journal/articleDetail?nodeId=NODE09324431)

---

## 3. 변수 정리

### Y

환승 성공 여부. 실측 도착 시각에서 시간표 도착 시각을 뺀 지연 D를, 시간표와 환승 소요시간으로 계산한 버퍼 B와 비교해 D < B이면 1.

### 지연 분포 F_D의 조건 변수

| 변수 | 출처 | 상태 | 비고 |
|---|---|---|---|
| 노선 | 시간표 `호선`, 실시간 `subwayId` | 있음 | 풀링의 기본 단위 |
| 방향 | 시간표 `방향`, 실시간 `updnLine` | 있음 | 노선별 대응 확인 필요 (4장 참고) |
| 요일 유형 | 수집 날짜 + 공휴일 캘린더 | 캘린더 필요 | 평일 / 주말·공휴일 |
| 시간대 | 시간표 `열차도착시간` | 있음 | 22시대·23시대·24시대 |
| 급행 여부 | 시간표 `급행여부`, 실시간 `directAt` | 있음 | 9호선 급행은 별도 분포 |
| 행선(종착역) | 시간표 `도착역`, 실시간 `statnTnm` | 있음 | 노선 내 분기 구분 |
| 막차로부터 순서 | 시간표에서 계산 | 계산 필요 | 지연 누적 여부 검증용 |
| 배차 간격 | 시간표에서 계산 | 계산 필요 | 회복 여유 |

역 단위는 조건에 넣지 않습니다. 노선 분포를 그대로 쓴다고 명시합니다.

### 버퍼 B (결정론적, 이미 346개 조합 산출)

| 변수 | 출처 |
|---|---|
| 도착 노선 막차 도착 시각 | 시간표 |
| 환승 노선 막차 출발 시각 | 시간표 |
| 환승 도보시간 | 환승 CSV (보행속도 1.2m/s 환산값) |
| 환승 거리 | 환승 CSV |

### 밤 단위 공변량

| 변수 | 출처 | 상태 | 판단 |
|---|---|---|---|
| 이상상황 발생 여부 | 알림정보 `getNtceList` | 있음 | 그 밤 그 노선에 사고·지연 공지가 있었는지 이진 플래그 |
| 강수 여부 | 기상청 | 없음 | 밤 단위 이진으로 충분. 키 발급 필요 |
| 혼잡도 | 분기별 스냅샷 | 없음 | 밤마다 안 변해 조건 변수로 기능하지 않음. 제외 |

### 가중치·서비스화용 (모형 밖)

| 변수 | 출처 | 쓰임 |
|---|---|---|
| 심야 귀가 이동량 | 생활이동 `move_purpose=3`, 22~01시 | 어느 환승역 실패가 몇 명에게 영향 주는지 |
| 올빼미버스 심야 승차 | `CardBusTimeNew` N노선 14개 | 실패 시 대안 수요 실측 |
| 정류장 좌표 | `busStopLocationXyInfo` | 역에서 올빼미버스 정류장까지 거리 |
| 따릉이 잔여 대수 | `bikeList` | 실패 시 제3 대안 |
| 역 좌표 | `subwayStationMaster` | 프로토타입 지도 |

---

## 4. 데이터 현황

### 확보·검증 완료 (13종)

| 데이터 | 규모 | 비고 |
|---|---|---|
| 열차운행시각표 파일 | 423,107행 · 458개 역 · 평일/토/휴일 | **기준 시간표.** API 수집분과 전수 대조 완료 |
| 시간표 (API 수집분) | 평일 159,263 · 토 131,926 · 휴일 100,758행 | 파일과 대조용. 휴일은 상한에 걸려 343개 역까지 |
| 역 마스터 | 799건 | 역코드·역외부코드·노선 |
| 역 좌표 | 784건 | 역코드로 마스터와 768건 매칭 |
| 환승역 거리·소요시간 | 140건 | CSV |
| 지하철 시간대별 승하차 | 1,242행 | 0~23시 승·하차 분리 |
| 지하철 일별 승하차 | 617행 | |
| 버스 시간대별 승하차 | 43,171행 | 올빼미버스 14개 노선 4,658행 분리 가능 |
| 버스정류장 좌표 | 11,236건 | |
| 따릉이 대여소 마스터 | 3,244건 | |
| 따릉이 실시간 잔여 | 1,000행 | |
| 수도권 생활이동 | 77MB · 31일 · 행정동 658개 | 심야 이동의 대부분이 귀가 목적 |
| 지하철 알림정보 | 1,000건 · 2021~2026 | 서비스명 `getNtceList` |

### 시간표 대조 결과 (파일 vs API)

역·요일·방향·열차·출발시각 다섯 키로 전수 대조했습니다.

| 요일 | 파일 | API | 키 일치 | 불일치 원인 |
|---|---:|---:|---:|---|
| 평일 | 159,263 | 159,263 | 99.8% | 방향 라벨 251건 |
| 토요일 | 131,926 | 131,926 | 92.5% | 방향 라벨 9,870건 |
| 휴일 | 131,918 | 100,758 | 99.9% | API가 343개 역까지만 받음 |

불일치는 전부 방향 라벨 차이입니다. 같은 역·같은 열차코드·같은 출발시각인데 파일은 `DOWN`, API는 상행(1)으로 적는 식입니다. 6호선 응암순환 구간(역코드 2611)과 9호선 토요일 전체에서 그렇습니다. 시각이 다른 행은 한 건도 없었습니다. 두 소스가 독립적으로 같은 값을 주므로 시간표는 검증된 것으로 봅니다.

**방향 코드는 노선별로 대응을 확인하고 붙입니다.** 파일은 `UP/DOWN/IN/OUT`, API는 `INOUT_TAG` 1/2입니다. 대부분 `UP→1`, `DOWN→2`이고 2호선 순환선은 `OUT→1`, `IN→2`입니다. 6호선 응암순환과 9호선 토요일은 소스마다 반대입니다. 실시간 `updnLine`을 시간표에 붙일 때는 노선별로 열차코드 겹침을 세어 어느 쪽이 맞는지 확인한 뒤 붙입니다.

**토요일과 휴일 시간표는 같습니다.** 두 요일이 다 있는 조합 681개 중 673개(98.8%)가 열차번호까지 완전히 동일합니다. 요일은 평일과 주말 두 가지로 다룹니다.

**토요일 막차가 평일보다 약 50분 이릅니다.** 중앙값이 평일 24:44, 토요일 23:54입니다. 2호선과 9호선은 정확히 한 시간 차이이고, 마지막 열차뿐 아니라 그 앞 열차들도 같은 폭으로 당겨져 있어 시간표 전체가 이동한 구조입니다. 1호선만 차이가 작은데 코레일 구간이 섞여 운영 주체가 다르기 때문으로 보입니다.

### 못 쓰는 것

**노선별 지연시간 정보 (지연증명서).** 1,869행, 1년치. 시간대 구분이 첫차~09시(82%), 09시~18시, 18시~막차(40건) 세 개뿐이라 22시~01시를 짚어낼 수 없습니다. 실제 지연이 아니라 지연증명서 발급 수요를 재는 데이터입니다. 검증용으로는 못 쓰고, "기존 공개 데이터로는 심야 지연을 알 수 없다"는 근거로만 인용합니다.

### 미확보

**기상청 종관기상관측(ASOS).** 공공데이터포털 별도 키가 필요합니다. 강수 여부를 밤 단위 공변량으로 넣기로 하면 그때 발급받습니다.

### 대기 중

**실시간 도착정보.** 오늘 밤 22시부터 4주, 수집 창은 **22:00~02:00, 3분 간격**. 일괄 조회 1콜로 19개 노선 560개 역 약 2,900행.

창의 끝을 2시로 잡은 근거는 운행시각표입니다. 평일 최종 도착이 25:14(새벽 1:14)이고 2·4·7·9호선이 1시 이후에 종착합니다. 1시에 끊으면 막차의 종착 도착을 놓칩니다. 지연이 붙으면 더 늦어지므로 여유를 뒀습니다. 시작은 가장 이른 역의 막차가 평일 23:28, 토요일 23:24라 22시면 한 시간 반 앞서 지연 누적 과정을 볼 수 있습니다.

| 요일 | 최종 출발 | 최종 도착 | 가장 이른 역의 막차 |
|---|---|---|---|
| 평일 | 25:11 (2호선) | 25:14 (2호선) | 23:28 (1호선) |
| 토요일 | 24:40 (1호선) | 24:42 (1호선) | 23:24 (4호선) |

간격을 3분으로 늘린 것은 한도 때문입니다. 4시간을 2분으로 돌리면 1,200콜이라 1,000을 넘습니다. 3분이면 800콜입니다. 폴링 간격이 늘어난 만큼 `barvlDt` 보간이 더 중요해졌습니다. 작업 스케줄러에 매일 21시 55분으로 등록했고 실행까지 확인했습니다. 배터리 전원일 때 실행을 막는 기본 설정 때문에 처음엔 대기 상태로 멈춰 있었고, 이를 풀어서 해결했습니다.

---

## 5. 수집 일정과 공휴일

수집 기간 9/16 ~ 10/13, 28밤. 이 안에 달력상 평일인데 휴일 시간표로 도는 밤이 넷 있습니다.

| 날짜 | 요일 | 사유 |
|---|---|---|
| 9/24 | 수 | 추석 연휴 |
| 9/25 | 목 | 추석 |
| 10/5 | 월 | 개천절 대체공휴일 |
| 10/9 | 금 | 한글날 |

이 밤들을 평일로 라벨링하면 시간표부터 틀립니다. **평일 시간표 밤은 16, 주말·공휴일 밤은 12입니다.**

---

## 6. 첫날 밤 데이터로 먼저 확인할 것

순서대로입니다. 앞의 것이 안 풀리면 뒤는 의미가 없습니다.

1. **열차번호 매칭.** 실시간 `btrainNo`(예: `0676`)와 시간표 `열차코드`(예: `K1902`, `S914`)의 형식이 다릅니다. 앞 글자가 운영 구간 코드인지, 뒤 숫자만 맞추면 되는지 확인. 안 붙으면 열차별 지연이 아니라 역별 다음 열차 도착 간격으로 우회.
2. **`barvlDt` 보간.** 폴링 시각 + 남은 초로 계산한 도착 시각이 분 단위 아래로 맞는지. 3분 폴링이라 이게 안 되면 측정 오차가 최대 3분이 된다. 안 되면 위치 호출을 빼고 일괄 조회만 1분 간격으로 돌리는 쪽으로 바꾼다(240콜).
3. **역코드 매핑.** 실시간 `1001000133` ↔ 시간표 `0150`/`133`. 끝 세 자리 규칙이 보이지만 `P148` 같은 문자 코드가 있어 799건 전수 대조.
4. **방향 대응.** 노선별로 열차코드 겹침을 세어 `updnLine` ↔ 시간표 방향 확정.
5. **중복 처리 기준.** 같은 열차가 생성 시각만 다른 두 레코드로 오는 경우가 있음. 최신만 남길지 둘 다 쓸지 결정하고 코드에 명시.

---

## 7. 파일 위치

```
lastcall-collector/
├─ collect.py                  야간 수집기 (스케줄러가 매일 21:55 실행)
├─ fetch_reference.py          참조 데이터 수집
├─ fetch_aux.py                보조 데이터 수집 + 판정
├─ eda_timetable.py            시간표 EDA
├─ utils.py                    시각 파싱, 운영일 계산, .env 로더
├─ EDA_시간표.md                1차 EDA 결과
├─ 데이터_사용판정.md            데이터별 상세 판정
├─ 프로젝트_방향_결정.md          이 문서
└─ data/
   ├─ reference/               시간표(파일·API), 역 마스터, 좌표, 환승, 지연증명서
   ├─ aux/                     승하차, 버스, 따릉이, 알림, 생활이동
   ├─ raw/                     실시간 (오늘 밤부터)
   └─ eda/                     그래프, CSV
```

`data/`와 `.env`는 git에 올라가지 않습니다.
